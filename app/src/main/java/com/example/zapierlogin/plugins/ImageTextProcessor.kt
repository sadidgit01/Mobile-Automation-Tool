package com.example.zapierlogin.plugins

class ImageTextProcessor {
    fun extractTextFromImage(imagePath: String): String {
        // This is just a placeholder. Actual OCR logic will be added later.
        return "Pretend we extracted text from: $imagePath"
    }
}
