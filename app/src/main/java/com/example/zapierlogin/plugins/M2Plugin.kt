package com.example.zapierlogin.plugins

class M2Plugin {
    fun runPlugin(input: String): String {
        // This is a placeholder logic. Real plugin logic will replace this.
        return "M2Plugin received input: $input"
    }
}
